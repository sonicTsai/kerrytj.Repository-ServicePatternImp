@model IEnumerable<RepositoryServicePatternDemo.Core.Models.FoodItem>
@{
    ViewData["Title"] = "Index";
}

<h1>Food Items Sold</h1>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Name)</th>
            <th>@Html.DisplayNameFor(m => m.SalePrice)</th>
            <th>@Html.DisplayNameFor(m => m.UnitPrice)</th>
            <th>Profit Per Item</th>
            <th>@Html.DisplayNameFor(m => m.Quantity)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(m => item.Name)</td>
                <td>@Html.DisplayFor(m => item.SalePrice)</td>
                <td>@Html.DisplayFor(m => item.UnitPrice)</td>
                <td>$@(item.SalePrice - item.UnitPrice)</td>
                <td>@Html.DisplayFor(m => item.Quantity)</td>
            </tr>
        }
    </tbody>
</table>

